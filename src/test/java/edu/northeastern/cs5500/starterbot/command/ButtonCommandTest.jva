package edu.northeastern.cs5500.starterbot.command;
import static com.google.common.truth.Truth.assertThat;

//import edu.northeastern.cs5500.starterbot.controller.BlackjackController;
import org.junit.jupiter.api.Test;

public class ButtonCommandTest {
    @Test
    void testSlashCommandHandlerAndButtonHandlerImplementation() {
        ButtonCommand buttonCommand = new ButtonCommand();
        assertThat(buttonCommand).isInstanceOf(SlashCommandHandler.class);
        assertThat(buttonCommand).isInstanceOf(ButtonHandler.class);
    }
}
